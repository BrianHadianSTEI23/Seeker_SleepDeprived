import { CountryData } from "@/app/_types/types"

const CountryHighlight = ({
    countryData,
    onCountryClick
}: {
    countryData?: CountryData[], 
    onCountryClick?: (country: CountryData) => void 
}) => {
    return (
        <div>
            
        </div>
    )
}